import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.asdecom.22328',
  appName: 'Asdecom',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
